# Copy to .env for `docker compose` overrides (optional).
# Defaults in docker-compose.yml are sufficient for local dev.

POSTGRES_USER=koyn
POSTGRES_PASSWORD=koyn_dev
POSTGRES_DB=koyn

JWT_SECRET=change-me-before-any-public-deploy
CHAIN_ID=8453
CORS_ORIGINS=http://localhost:3000
REQUIRED_CLIENT_BUILD=1
ALPHA_PUBLIC=0

NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_CHAIN_ID=8453
NEXT_PUBLIC_CLIENT_BUILD=1
